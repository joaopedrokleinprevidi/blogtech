/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.google.com",
      "wpcdn.web.wsu.edu",
      "files.tecnoblog.net",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
